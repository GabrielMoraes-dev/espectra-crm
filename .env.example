AUTOMACAO_SECRET=

# Trava explícita da Z-API (WhatsApp) — só o valor exato "true" ativa o envio real.
# Ausência da variável (ou qualquer outro valor) mantém a integração pausada,
# mesmo que ZAPI_INSTANCE_ID/ZAPI_TOKEN/ZAPI_CLIENT_TOKEN estejam configurados.
ZAPI_ENABLED=
